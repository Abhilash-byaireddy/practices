sentence = input("Enter a sentence:")

# Split the sentence into a list of words based on whitespace
words = sentence.split()

# Get the count of elements in that list
word_count = len(words)

print(f"Word count: {word_count}")
